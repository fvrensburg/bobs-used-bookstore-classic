// BookstoreConfiguration has been removed.
// All configuration is now read via the standard IConfiguration abstraction injected by
// the ASP.NET Core DI container. Web.config <appSettings> values are in appsettings.json.
// AWS SSM Parameter Store overrides are loaded into IConfiguration at startup in Program.cs.
